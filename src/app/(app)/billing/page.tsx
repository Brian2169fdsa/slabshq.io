import { Phase2Shell } from '@/components/Phase2Shell';
export default function BillingPage() {
  return <Phase2Shell title="Billing & Plans" blurb="Subscription, usage metering, invoices. Depends on Stripe + the metering migration (006) — confirm against the backend." />;
}
